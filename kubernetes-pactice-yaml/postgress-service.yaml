apiVersion: v1
kind: Service
metadata:
   name: db
   labels:
     name: postgress-Service
     app: demo-voting-app
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    name: postgress-pod
    app: demo-voting-app