apiVersion: v1
kind: Service
metadata:
   name: voting-Service
   labels:
     name: voting-service
     app: demo-voting-app
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodeport: 30004
  selector:
    name: voting-app-pod
    app: demo-voting-app